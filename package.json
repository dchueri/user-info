{
  "name": "user-info",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "git@github.com:dchueri/user-info.git",
  "author": "diego <diegochueri@gmail.com>",
  "license": "MIT",
  "scripts": {
    "env:generate": "cp ./infra/.env.example ./infra/.env && cp ./server/.env.example ./server/.env",
    "docker:build": "cd ./infra && docker-compose build",
    "docker:up": "cd ./infra && docker-compose up"
  } 
}
